"""The nine component registries.

Import a registry module directly (``from neural_trade.registries.losses import Losses``);
each one is populated on import. ``load_all()`` / ``ALL_REGISTRIES`` arrive once all nine
exist (Phase B4-B12).
"""
