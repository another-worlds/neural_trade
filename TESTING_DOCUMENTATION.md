# Test suite

_Generated by `scripts/test_inventory.py` from the collected suite - do not edit by hand._

299 tests in 36 files. Markers: `tf` 130, `slow` 12, `gpu` 0, `data` 0, `notebook` 1.

## Running

```bash
pytest -m "not slow"                  # fast suite (CPU, ~2 min)
pytest -m slow                        # end-to-end training, CLI, notebooks, reproducibility
pytest --cov=neural_trade --cov-report=json:coverage.json && python scripts/check_coverage.py coverage.json
python scripts/golden_run.py verify <oracle.npz>   # a refactor changed no numbers
```

Markers: `tf` imports TensorFlow (added automatically); `slow` trains a model or takes more than ~30 s; `gpu` needs a GPU; `data` reads the bundled CSV; `notebook` executes notebooks (needs ipykernel).

## Coverage gates (line coverage per package, `scripts/check_coverage.py`)

| package | gate |
|---|---|
| `calibration` | 80% |
| `core` | 95% |
| `data` | 75% |
| `evaluation` | 85% |
| `losses` | 85% |
| `models` | 50% |
| `strategy` | 85% |
| `training` | 50% |

## Files

| file | tests | markers | purpose |
|---|---|---|---|
| `tests/registries/test_base_hardening.py` | 8 |  | BaseRegistry hardening (plan section B3): per-subclass storage, strict mode, live exceptions, defaults, dependency checks, build(), explicit discovery. |
| `tests/registries/test_callbacks.py` | 4 | tf 4 | Callbacks registry: default order reproduces the old hard-coded list; schedule and threshold. |
| `tests/registries/test_contracts.py` | 3 |  | Every registry on the training path is actually queried during training; load_all and plugins. |
| `tests/registries/test_data_registries.py` | 5 |  | DataLoaders and Preprocessors registries; the default pipeline reproduces the old loader. |
| `tests/registries/test_layers.py` | 3 | tf 3 | Layers registry: the four custom layers are registered, strict, buildable by role. |
| `tests/registries/test_metrics.py` | 7 | tf 7 | Metrics registry: numpy evaluation tier and TF step tier, contracts and selection. |
| `tests/registries/test_models.py` | 4 | tf 4 | Models registry: gru_attention builds with the 10 PredictiveOutputs heads; contracts enforced. |
| `tests/registries/test_optimizers.py` | 3 | tf 3 | Optimizers registry and the optimizer pair of a run. |
| `tests/registries/test_visualizations.py` | 4 |  | Visualizations registry and the model.py compatibility shim. |
| `tests/test_ablation_harness.py` | 6 | slow 1 | Ablation harness (plan C4): grid construction, resume, verdict rules, and one real cell. |
| `tests/test_backtest.py` | 27 |  | Backtest engine and strategies (plan C5): hand-computed P&L after costs, stop/target on the bar's high/low, next-open fills, look-ahead self-test for every registered strategy, causal features. |
| `tests/test_calibration.py` | 10 |  | Calibration package: the split guard, conformal coverage, temperature direction, save/load. |
| `tests/test_calibration_pass.py` | 2 |  | The pre-training lambda-calibration pass must restore the configured lambdas when it fails. |
| `tests/test_cli.py` | 5 | slow 1 | neural-trade CLI (plan B16): argument handling, registry/env commands, and a full train -> predict -> backtest round trip on synthetic bars. |
| `tests/test_config.py` | 26 |  | Typed Config (plan section B2): legacy names/defaults preserved, validation, override, YAML round trip, per-instance mutable defaults. |
| `tests/test_custom_loss.py` | 13 | tf 13 | Loss-system regression tests at REALISTIC scales. |
| `tests/test_data_processor.py` | 4 |  | Purged four-way split, scaler hygiene, window-relative input and target alignment (M3/M4). |
| `tests/test_direction_labels.py` | 2 | tf 2 | One direction-labelling rule everywhere: NumPy and TF twins agree, including at the deadband edges, and the Gaussian readouts agree too. |
| `tests/test_direction_readout.py` | 8 | tf 8 | The Gaussian direction readout P(up | |delta| > d) and its stable log-CDF. |
| `tests/test_epoch_metrics.py` | 4 | tf 4 | Logged step metrics are EPOCH aggregates, not the last batch. |
| `tests/test_eval_metrics.py` | 6 |  | Evaluation protocol (plan C2): metric definitions, baselines, confidence gap, n_eff. |
| `tests/test_hygiene.py` | 5 |  | Code hygiene (plan B16): no print() in the package, logging reaches the current stdout, and ``import neural_trade`` stays cheap (no TensorFlow). |
| `tests/test_learnable_indicators.py` | 9 | tf 9 | LearnableIndicators: EWMA equivalence (matrix vs scan), output shape, gradient reach. |
| `tests/test_losses.py` | 3 | tf 3 | The Losses registry: what is registered, the logging helper, and that it hands out production code. |
| `tests/test_losses_reference.py` | 34 | tf 34 | Production loss functions vs independent numpy references (plan C1). |
| `tests/test_notebook_ui.py` | 8 | slow 4 | Notebook front-ends (neural_trade.notebook) and their figures, driven headlessly. |
| `tests/test_notebooks_thin.py` | 14 | slow 1, notebook 1 | Notebooks are thin (plan B17): no def/class, outputs stripped, and all four execute end to end. |
| `tests/test_physics_terms_bounded.py` | 10 | tf 10 | M2 regression tests: every physics-inspired term is bounded and pulls the right way, lambdas are live tf.Variables, ECE uses the positive-class convention, and period clipping never writes a saturated logit. |
| `tests/test_predictor_round_trip.py` | 3 | tf 3, slow 3 | Train -> save the artifact bundle -> load a Predictor -> identical predictions (plan B5/B13). |
| `tests/test_qbox_losses.py` | 15 | tf 15 | The physics-inspired (QBOX / T_perp) loss terms, called through the production registry. |
| `tests/test_registry_base.py` | 30 |  | Unit tests for base registry functionality. |
| `tests/test_reproducibility.py` | 2 | tf 2, slow 2 | Reproducibility (plan C6): same seed -> identical run; SEED given as an override takes effect. |
| `tests/test_telemetry.py` | 5 |  | JSONL epoch telemetry never raises; convergence score NaN stays NaN; RunContext layout. |
| `tests/test_train_smoke.py` | 3 | tf 3 | Three real training steps on synthetic bars. |
| `tests/test_training_wiring.py` | 3 | tf 3 | The training model resolves its objective and optimizers through the registries; ablation. |
| `tests/test_windowing.py` | 1 |  | Inference windows reach the newest bar and agree with the training windows. |
